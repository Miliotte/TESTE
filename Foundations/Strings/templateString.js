const name = 'Ricardo'
const concatenacao = 'Olá' + name + '!'
const template = `
    Olá
    ${name}!`

console.log(concatenacao, template)

// expressoes...

console.log(`1 + 1 = ${1 +1}`)

const up = texto => texto.toUpperCase()
console.log(`Eii...${up('cuidado')}`)

//toUpperCase = usado para deixar em CapsLock 